redux have store

interface CounterState{
    value: number;

    interface UserState{
        inSignedIn: boolean;
    }
}

have multiple slices in each stoer

//Action 

what to do with the store

const increment = {type :"INCREMENT", payload: 1}
type = name, required
payload = not required

//Reducers
Never directly mutate the state

